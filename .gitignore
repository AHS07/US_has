# ── Planning/reference docs (not part of the codebase) ──────────────────────
about/

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
eggs/
parts/
var/
sdist/
develop-eggs/
/lib/
/lib64/
!healthflow-ui/src/lib/
.eggs/
*.whl
MANIFEST

# ── Virtual environments ──────────────────────────────────────────────────────
venv/
.venv/
env/
ENV/
.env.local
.env.*.local

# ── Secrets / environment ─────────────────────────────────────────────────────
.env
# .env.example is intentionally NOT ignored — it belongs in the repo

# ── Django ────────────────────────────────────────────────────────────────────
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/
static_root/

# ── Frontend / Node ─────────────────────────────────────────────────────────
node_modules/
.npm
.yarn/
.pnpm-store/

# ── Coverage / testing ────────────────────────────────────────────────────────
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
pytestdebug.log

# ── Type checking ─────────────────────────────────────────────────────────────
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/

# ── Linting / formatting ──────────────────────────────────────────────────────
.ruff_cache/

# ── Docker ────────────────────────────────────────────────────────────────────
docker-compose.override.yml

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints
*.ipynb

# ── IDE / editor ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Node (frontend service, if any) ──────────────────────────────────────────
node_modules/
.npm
.yarn/

# ── Celery ────────────────────────────────────────────────────────────────────
celerybeat-schedule
celerybeat.pid

# ── Uploads (local dev) ───────────────────────────────────────────────────────
uploads/

# ── Medicine seed CSVs (large files, not committed) ───────────────────────────
healthflow/data/medicine_seed/*.csv

# ── Compiled assets ───────────────────────────────────────────────────────────
*.min.js
*.min.css

# ── Local test runners & benchmarks (not committed) ───────────────────────────
scenario_tester.py
healthflow/scenario_tester.py
live_benchmarks.py
healthflow/live_benchmarks.py
